if 'data_loader' not in globals():
    from mage_ai.data_preparation.decorators import data_loader
if 'test' not in globals():
    from mage_ai.data_preparation.decorators import test


@data_loader
def load_data(*args, **kwargs):
    years = [2022, 2023, 2024, 2025]

    jobs = []
    for y in years:
        for m in range(1, 13):
            jobs.append({
                "year": y,
                "month": m,
                "year_month": f"{y}-{m:02d}",
                "service_type": "yellow",
            })

    print("=" * 70)
    print("[START] Generando jobs para ingestión Yellow 2022-2025")
    print(f"[INFO] Total jobs: {len(jobs)} (debe ser 48)")
    print("[INFO] Ejemplo job:", jobs[0])
    print("=" * 70)

    return jobs


@test
def test_output(output, *args) -> None:
    assert output is not None, "El output es undefined"

    # Mage a veces envuelve el output (ej: {"output_0": [...]} o {"0": [...]})
    jobs = output
    if isinstance(output, dict):
        # toma el primer valor del dict
        jobs = next(iter(output.values()))
    elif isinstance(output, tuple):
        # toma el primer item si vino como tupla
        jobs = output[0]

    assert isinstance(jobs, list), f"Se esperaba una lista de jobs, llegó {type(jobs)}"
    assert len(jobs) == 48, f"Se esperaban 48 jobs, llegaron {len(jobs)}"
    assert all(
        ("year" in j and "month" in j and "year_month" in j and "service_type" in j)
        for j in jobs
    ), "Faltan llaves en uno o más jobs"

